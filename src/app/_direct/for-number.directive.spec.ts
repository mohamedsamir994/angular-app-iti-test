import { ForNumberDirective } from './for-number.directive';

describe('ForNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ForNumberDirective();
    expect(directive).toBeTruthy();
  });
});
